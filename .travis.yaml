language: node_js

node_js:
  - "10"

notifications:
  email:
    - josh.rossi@alphapoint.com

install:
  - npm install

before_script:
  - echo "before..."

script:
  - npm run build

after_script:
  - echo "after..."

deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: master