language: node_js

notifications:
  email:
    - josh.rossi@alphapoint.com

install:
  - npm install

script:
  - npm run build
  - git commit -a -m 'Updating Version'
  - git status
  - npm version patch
  - npm publish
  - git push
