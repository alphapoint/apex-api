language: node_js

notifications:
  email:
    - josh.rossi@alphapoint.com

script:
  - npm run build
  - nvm version patch
  - git commit -am 'Updating Version'
  - git push

deploy:
  provider: npm
  edge: true
  skip_cleanup: true
  email: "josh.rossi@alphapoint.com"
  api_key: $NPM_TOKEN
