language: node_js

notifications:
  email:
    - josh.rossi@alphapoint.com

install:
  - npm install

before_script:
  - "before..."

script:
  - npm run build
  - npm run publish

after_script:
  - "after..."

deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: master